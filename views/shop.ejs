<h1>Books for Sale</h1>
<ul>
    <% books.forEach(book => { %>
    <li>
        <%= book.title %> by <%= book.author %> - $<%= book.price %>
        <a href="/buy/<%= book._id %>">Buy</a>
    </li>
    <% }); %>
</ul>
